// Hueco reservado para el test del TEMA 21.
const topic21Curated=[]; window.topic21Curated=topic21Curated;
const previousTopicMenu21=window.topicMenu; window.topicMenu=function(){ previousTopicMenu21(); if(!ws||ws.hidden)return; const cards=ws.querySelector('.cards'); if(!cards)return; if([...cards.querySelectorAll('button.module-card')].some(b=>(b.textContent||'').includes('TEMA 21')))return; const button=document.createElement('button'); button.className='module-card'; button.type='button'; button.disabled=true; button.innerHTML=`<span class="module-number">TEMA 21</span><h3>${esc((topics[21]||{}).title||'TEMA 21')}</h3><p>0 preguntas · en preparación</p><span class="module-action">Próximamente</span>`; const before=[...cards.querySelectorAll('button.module-card')].find(b=>{const m=(b.textContent||'').match(/TEMA\s+(\d+)/);return m&&Number(m[1])>21;}); if(before)cards.insertBefore(button,before);else cards.appendChild(button); };