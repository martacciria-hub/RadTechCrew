// Hueco reservado para el test del TEMA 22.
const topic22Curated=[]; window.topic22Curated=topic22Curated;
const previousTopicMenu22=window.topicMenu; window.topicMenu=function(){ previousTopicMenu22(); if(!ws||ws.hidden)return; const cards=ws.querySelector('.cards'); if(!cards)return; if([...cards.querySelectorAll('button.module-card')].some(b=>(b.textContent||'').includes('TEMA 22')))return; const button=document.createElement('button'); button.className='module-card'; button.type='button'; button.disabled=true; button.innerHTML=`<span class="module-number">TEMA 22</span><h3>${esc((topics[22]||{}).title||'TEMA 22')}</h3><p>0 preguntas · en preparación</p><span class="module-action">Próximamente</span>`; const before=[...cards.querySelectorAll('button.module-card')].find(b=>{const m=(b.textContent||'').match(/TEMA\s+(\d+)/);return m&&Number(m[1])>22;}); if(before)cards.insertBefore(button,before);else cards.appendChild(button); };