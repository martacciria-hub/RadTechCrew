// Hueco reservado para el test del TEMA 24.
const topic24Curated=[]; window.topic24Curated=topic24Curated;
const previousTopicMenu24=window.topicMenu; window.topicMenu=function(){ previousTopicMenu24(); if(!ws||ws.hidden)return; const cards=ws.querySelector('.cards'); if(!cards)return; if([...cards.querySelectorAll('button.module-card')].some(b=>(b.textContent||'').includes('TEMA 24')))return; const button=document.createElement('button'); button.className='module-card'; button.type='button'; button.disabled=true; button.innerHTML=`<span class="module-number">TEMA 24</span><h3>${esc((topics[24]||{}).title||'TEMA 24')}</h3><p>0 preguntas · en preparación</p><span class="module-action">Próximamente</span>`; const before=[...cards.querySelectorAll('button.module-card')].find(b=>{const m=(b.textContent||'').match(/TEMA\s+(\d+)/);return m&&Number(m[1])>24;}); if(before)cards.insertBefore(button,before);else cards.appendChild(button); };