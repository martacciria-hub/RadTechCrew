// Hueco reservado para el test del TEMA 20.
const topic20Curated=[]; window.topic20Curated=topic20Curated;
const previousTopicMenu20=window.topicMenu; window.topicMenu=function(){ previousTopicMenu20(); if(!ws||ws.hidden)return; const cards=ws.querySelector('.cards'); if(!cards)return; if([...cards.querySelectorAll('button.module-card')].some(b=>(b.textContent||'').includes('TEMA 20')))return; const button=document.createElement('button'); button.className='module-card'; button.type='button'; button.disabled=true; button.innerHTML=`<span class="module-number">TEMA 20</span><h3>${esc((topics[20]||{}).title||'TEMA 20')}</h3><p>0 preguntas · en preparación</p><span class="module-action">Próximamente</span>`; const before=[...cards.querySelectorAll('button.module-card')].find(b=>{const m=(b.textContent||'').match(/TEMA\s+(\d+)/);return m&&Number(m[1])>20;}); if(before)cards.insertBefore(button,before);else cards.appendChild(button); };