// Hueco reservado para el test del TEMA 25.
const topic25Curated=[]; window.topic25Curated=topic25Curated;
const previousTopicMenu25=window.topicMenu; window.topicMenu=function(){ previousTopicMenu25(); if(!ws||ws.hidden)return; const cards=ws.querySelector('.cards'); if(!cards)return; if([...cards.querySelectorAll('button.module-card')].some(b=>(b.textContent||'').includes('TEMA 25')))return; const button=document.createElement('button'); button.className='module-card'; button.type='button'; button.disabled=true; button.innerHTML=`<span class="module-number">TEMA 25</span><h3>${esc((topics[25]||{}).title||'TEMA 25')}</h3><p>0 preguntas · en preparación</p><span class="module-action">Próximamente</span>`; const before=[...cards.querySelectorAll('button.module-card')].find(b=>{const m=(b.textContent||'').match(/TEMA\s+(\d+)/);return m&&Number(m[1])>25;}); if(before)cards.insertBefore(button,before);else cards.appendChild(button); };